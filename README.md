# wardenkernel

This is a minimal bare-metal kernel proving one guarantee — capability tokens can only narrow when delegated between processes, never widen — enforced by the kernel.

Not a general-purpose OS. Currently at Phase 1 (boot only).
